name: Shaell Commands
on: [push]
jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: eccSho "Hello World"
      - name: multiple scripts
        run: |
          node -v
          npm -v
